<div {{ attributes }}>
    {{ form_start(form) }}
    {{ form_row(form.currentPassword) }}
    {{ form_row(form.plainPassword) }}

    <div class="d-flex flex-column gap-2 align-items-center">
        <button type="submit" class="w-100 my-3">
            <i class="bi bi-pencil-square me-2"></i>
            {{ 'action.edit'|trans }}
        </button>
    </div>
    {{ form_end(form) }}
</div>