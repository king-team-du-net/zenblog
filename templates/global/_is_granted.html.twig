{% if is_granted('ROLE_PREVIOUS_ADMIN') %}
    {% include "global/_message.html.twig" with { type: 'info', icon: 'bi bi-info-circle', message: ('You are connected as "%nickname%'|trans({'%nickname%' : app.user.nickname})~ '", <a href="'~ path('dashboard_index', {'_switch_user': '_exit'}) ~'">' ~ 'Click here'|trans ~ '</a> ' ~ 'to return to your dashboard'|trans) } %}
{% endif %}